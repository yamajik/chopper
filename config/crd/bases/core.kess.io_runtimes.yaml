
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: runtimes.core.kess.io
spec:
  group: core.kess.io
  names:
    categories:
    - kess
    kind: Runtime
    listKind: RuntimeList
    plural: runtimes
    shortNames:
    - rt
    singular: runtime
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .spec.command
      name: Command
      priority: 10
      type: string
    - jsonPath: .spec.functions[*].name
      name: Functions
      type: string
    - jsonPath: .spec.libraries[*].name
      name: Libraries
      type: string
    - jsonPath: .status.functions
      name: Functions_Hash
      priority: 10
      type: string
    - jsonPath: .status.libraries
      name: Libraries_Hash
      priority: 10
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Runtime is the Schema for the runtimes API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RuntimeSpec defines the desired state of Runtime
            properties:
              clusterIP:
                description: Optional cluster IP spec of runtime
                type: string
              command:
                description: The container image of runtime
                items:
                  type: string
                type: array
              env:
                description: The container image of runtime
                items:
                  description: EnvVar represents an environment variable present in a Container.
                  properties:
                    name:
                      description: Name of the environment variable. Must be a C_IDENTIFIER.
                      type: string
                    value:
                      description: 'Variable references $(VAR_NAME) are expanded using the previous defined environment variables in the container and any service environment variables. If a variable cannot be resolved, the reference in the input string will be unchanged. The $(VAR_NAME) syntax can be escaped with a double $$, ie: $$(VAR_NAME). Escaped references will never be expanded, regardless of whether the variable exists or not. Defaults to "".'
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?'
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                        fieldRef:
                          description: 'Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels[''<KEY>'']`, `metadata.annotations[''<KEY>'']`, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.'
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                        resourceFieldRef:
                          description: 'Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.'
                          properties:
                            containerName:
                              description: 'Container name: required for volumes, optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must be a valid secret key.
                              type: string
                            name:
                              description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?'
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                type: array
              functions:
                description: The functions of runtime
                items:
                  description: RuntimeFunctions bulabula
                  properties:
                    mount:
                      default: /app/fn/{Name}
                      description: The functions of runtime
                      type: string
                    name:
                      description: The functions of runtime
                      type: string
                  type: object
                type: array
              image:
                description: The container image of runtime
                type: string
              libraries:
                description: The libraries of runtime
                items:
                  description: RuntimeLibraries bulabula
                  properties:
                    mount:
                      default: /app/lib/{Name}
                      description: The functions of runtime
                      type: string
                    name:
                      description: The functions of runtime
                      type: string
                  type: object
                type: array
              port:
                default: 8000
                description: Optional port for runtime.
                format: int32
                maximum: 65535
                minimum: 0
                type: integer
              portName:
                default: http
                description: Optional port for runtime.
                type: string
              readyFormat:
                default: '{AvailableReplicas}/{AvailableReplicas}'
                description: Optional ready format spec of runtime
                type: string
              replicas:
                default: 1
                description: Optional cluster IP spec of runtime
                format: int32
                minimum: 0
                type: integer
            type: object
          status:
            description: RuntimeStatus defines the observed state of Runtime
            properties:
              functions:
                additionalProperties:
                  type: string
                description: Optional functions string of runtime for show
                type: object
              libraries:
                additionalProperties:
                  type: string
                description: Optional libraries string of runtime for show
                type: object
              ready:
                description: Optional ready string of runtime for show
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
